[project]
name = "claude-fleet-mcp"
version = "0.1.0"
description = "MCP server for Claude Code fleet monitoring"
requires-python = ">=3.10"
dependencies = ["mcp[cli]>=1.0.0"]

[project.scripts]
claude-fleet-mcp = "server:main"
